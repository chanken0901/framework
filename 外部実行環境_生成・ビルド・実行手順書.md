# 外部実行環境 生成・ビルド・実行手順書

**対象:** 研究フレームワークの `NSE` / `GPE` ソルバー  
**版:** 2.7  
**更新日:** 2026-08-19  
**想定環境:** Windowsワークステーション、Linuxスパコン（Slurm）

---

## 1. この手順書の目的

この手順書は、ローカルの `C:\Users\Owner\Documents\Codex\FrameWork` を編集元として使い、ワークステーションまたはスパコンに計算に必要なファイルだけをコピーした実行環境を生成し、case作成、入力生成、ビルド、実行、結果確認まで行うためのものです。NASはローカルFrameWorkの同期ミラーとして使用し、NAS上のファイルを直接編集しません。

基本方針は次のとおりです。

- ローカルの `FrameWork` をソースコードと手順書の編集元とする。
- NASは同期先・バックアップ・他端末への受け渡し用ミラーとして扱う。
- ビルドと計算は、`FrameWork`の外に生成したローカル実行領域またはスパコンのスクラッチ領域で行う。
- 実行環境の構成は、GPEでは `environment.gpe.yaml`、NSEではCPU/MPI版と単一GPU版のどちらも `environment.nse.yaml` の `select` で選択する。
- 選択肢の正本は `environment_options.yaml` とし、追加候補は別カタログYAMLで拡張する。
- 流れ場や物理条件は、各caseの `case.yaml` で管理する。
- CMakeの設定値は設計書から生成し、通常運用では `CMakeLists.txt` を直接編集しない。
- 生成済み実行環境は再生成できる作業物として扱い、確定した実装変更はローカルFrameWorkへ反映する。
- Git/GitHubは変更履歴、NAS同期はミラー作成という別の役割で運用する。

> **重要:** NAS上のファイルを直接編集、ビルド、実行しません。編集はローカルFrameWork、計算は外部実行環境、NASは同期ミラーという三層を分離します。

## 2. 全体の流れ

```text
NAS同期ミラー
\\Mozart\share\FrameWork
          ▲
          │ ソース・設計書・手順書を同期
          ▼
ローカルFrameWork（編集元）
C:\Users\Owner\Documents\Codex\FrameWork
  ├─ SolverLibrary
  └─ ScriptLibrary
      └─ RunEnvironment
          ├─ environment.gpe.yaml
          ├─ environment.nse.yaml
          └─ environment_options.yaml
          │
          │ prepare_environment.py がselectを解決
          ▼
外部実行環境（ビルド・計算場所）
C:\Users\Owner\ResearchRuns
  ├─ Designs                 設計書の正本
  ├─ case_index.csv          全モデル・全caseの共通台帳
  └─ gpe_case0001
      ├─ SolverLibrary
      ├─ ScriptLibrary
      ├─ config/machine.yaml
      ├─ cases/case0001/case.yaml
      ├─ cases/case0001/input.nml
      └─ tools/run_case.py
          │
          ├─ 検証
          ├─ CMake構成
          ├─ ビルド
          ├─ MPI実行
          └─ 結果出力
```

日常運用は、次の9段階です。

1. 同期状態とGitの作業ツリーを確認する。
2. 必要な場合だけNASミラーからローカルへ同期する。
3. ローカルFrameWorkでソース、設計書、手順書を編集する。
4. `--list-options` で利用可能な選択肢を確認する。
5. `--dry-run` 後に外部実行環境を生成する。
6. `case.yaml` の物理条件を確認または変更する。
7. `run_case.py` で検証、ビルド、実行を行う。
8. 結果、来歴、Gitコミットを保存する。
9. 確定したFrameWorkの変更をGitHubとNASミラーへ反映する。

## 3. 前提条件

### 3.1 ローカルFrameWork

ローカルに次の構成が配置されていることを確認します。

```text
C:\Users\Owner\Documents\Codex\FrameWork\
  ├─ SolverLibrary\
  │   ├─ NSE\
  │   └─ GPE\
  └─ ScriptLibrary\
      ├─ BuildSolver\
      ├─ RunEnvironment\
      └─ SetupCase\
```

最低限、次のファイルが必要です。

```text
ScriptLibrary\RunEnvironment\prepare_environment.py
ScriptLibrary\RunEnvironment\environment.gpe.yaml
ScriptLibrary\RunEnvironment\environment.nse.yaml
ScriptLibrary\RunEnvironment\environment_options.yaml
ScriptLibrary\BuildSolver\model_catalog.yaml
```

### 3.2 NAS同期ミラー

NAS上の同期先は次です。

```text
\\Mozart\share\FrameWork
```

ローカルFrameWorkを編集元とし、NAS側は同期ミラーとして扱います。同期開始前に、コピー方向と削除反映の有無を必ず確認します。

| 同期対象 | 原則 |
|---|---|
| `SolverLibrary` | ローカルで確定したソースをNASへ同期 |
| `ScriptLibrary` | ローカルで確定したスクリプトと設計書をNASへ同期 |
| 手順書・管理台帳 | ローカルで更新後、NASへ同期 |
| `build`、実行結果、キャッシュ | FrameWork同期の対象外 |
| 一時ロックファイル `~$*` | 同期しない |

> **注意:** Git操作中、WordやExcelの編集中、ビルド中に自動同期を走らせません。同期は作業の区切りで実行します。

### 3.3 Windowsワークステーション側

次のソフトウェアを準備します。

| 項目 | 用途 | 確認コマンド |
|---|---|---|
| Python 3 | 環境生成と実行制御 | `python --version` |
| CMake | ビルドシステム生成 | `cmake --version` |
| Fortranコンパイラ | NSE/GPEのコンパイル | `gfortran --version` |
| MPI | MPIプロファイルの実行 | `mpiexec -help` |
| FFTW | FFTWプロファイルで使用 | 配置先を `machine.yaml` に指定 |
| CUDA Toolkit | CUDAプロファイルで使用 | `nvcc --version` |

Pythonが `python` で起動しない環境では、`py` またはPython実行ファイルの絶対パスを使います。

### 3.4 スパコン側

次の機能またはソフトウェアが必要です。

- Python 3
- CMake
- Fortranコンパイラ
- MPI実装
- 選択したFFTバックエンド
- Slurmを使う場合は `sbatch`
- GPUプロファイルではCUDA Toolkit
- cuFFTMpプロファイルでは、対応するCUDA、MPI、cuFFTMp環境

> **注意:** cuFFTMpプロファイルはLinux系の対応環境を前提とします。Windowsワークステーション用ではありません。

## 4. Windowsワークステーションでの最短手順

ここではGPEのMPI版を4プロセスで実行する例を示します。PowerShellを使用してください。

### Step 1: ローカルFrameWorkと選択肢を確認する

```powershell
$framework = "C:\Users\Owner\Documents\Codex\FrameWork"
$tool = "$framework\ScriptLibrary\RunEnvironment"
$designs = "$env:USERPROFILE\ResearchRuns\Designs"

New-Item -ItemType Directory -Force $designs | Out-Null
Copy-Item "$tool\environment.gpe.yaml" `
  "$designs\gpe_case0001.yaml"
```

利用できる選択肢を確認します。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --list-options
```

モデルだけを確認する場合は次を使います。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --list-options model
```

`$designs\gpe_case0001.yaml` を開き、選択IDを確認します。

```yaml
schema_version: 1
environment_id: gpe_case0001
option_catalogs: []

select:
  source: local_framework
  destination: windows_research_runs
  model: gpe_cpu_mpi_dft
  target: windows_gnu_msmpi
  case: gpe_quantum_taylor_green
  execution: release
  scheduler: disabled
  archive: none

parallel:
  use_mpi: true
  use_openmp: false
  use_cuda: false

case:
  id: case0001
  label: qtgv_baseline
  description: Quantum Taylor-Green baseline
```

`local_framework`は次のローカル編集元を参照します。

```text
C:\Users\Owner\Documents\Codex\FrameWork
```

`windows_research_runs`では、通常は生成先を上書きしません。次の親ディレクトリを使い、
空いている`gpe_caseNNNN`または`nse_caseNNNN`を自動採番します。

```yaml
destination:
  root: ${USERPROFILE}/ResearchRuns
  case_index: ${USERPROFILE}/ResearchRuns/case_index.csv
  auto_case_number: true
```

別ドライブへ変更する場合は、実行環境の親ディレクトリと共通台帳を一緒に上書きします。

```yaml
destination:
  root: D:/ResearchRuns
  case_index: D:/ResearchRuns/case_index.csv
```

> **推奨:** FrameWorkの中を生成先にせず、ビルド結果と計算結果を同期対象から分離します。

### Step 2: 生成内容を事前確認する

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --dry-run
```

表示されたモデル、プロファイル、コピー元、出力先を確認します。`--dry-run` ではファイルを生成しません。

### Step 3: 外部実行環境を生成する

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml"
```

生成先へ移動します。

```powershell
Set-Location "$env:USERPROFILE\ResearchRuns\gpe_case0001"
```

### Step 4: 生成結果を確認する

```powershell
Get-ChildItem
Get-Content .\README.md
```

次のファイルが存在することを確認します。

```text
.generated_run_environment.json
environment.lock.json
environment.source.yaml
environment.resolved.yaml
provenance.json
config\machine.yaml
cases\case0001\case.yaml
tools\run_case.py
```

実行環境の外側に、次の共通台帳が存在することも確認します。

```text
C:\Users\Owner\ResearchRuns\case_index.csv
```

このCSVは全モデル・全caseを1行ずつ保持します。`grid.nx`、`time.dt`、
`physics.gpe.alpha`など、`case.yaml`の全条件が階層名付きの列として記録されます。
各実行環境の`cases`内には個別の`case_index.csv`を置きません。

### Step 5: case条件を確認する

物理条件、格子数、時間刻み、初期条件、出力条件は次のファイルで管理します。

```text
cases\case0001\case.yaml
```

流れ場を変更するときも、まずこのファイルを編集します。生成された `input.dat` や `input.nml` を直接編集すると、次回の再生成で変更が失われます。
編集後の条件は、`run_case.py --prepare`、検証、ビルド、テスト、実行の開始時に
外側の共通`case_index.csv`へ自動反映されます。

### Step 6: ソルバー入力を生成する

```powershell
python .\tools\run_case.py --prepare
```

`environment.lock.json` からモデル、プロファイル、caseを取得し、`case.yaml` から `input.dat` または `input.nml` を生成します。通常の検証、ビルド、実行でも、`case.yaml` の方が入力ファイルより新しければ自動再生成されます。

### Step 7: 設定を検証する

```powershell
python .\tools\run_case.py --validate-only
```

エラーが出た場合は、主に次を確認します。

- `config\machine.yaml` のコンパイラとライブラリのパス
- `cases\case0001\case.yaml` の必須項目
- MPIプロセス数と格子分割の整合
- 選択したモデルとプロファイル

### Step 8: ビルドする

```powershell
python .\tools\run_case.py --build
```

クリーンビルドが必要な場合は次を使います。

```powershell
python .\tools\run_case.py --build --clean-first
```

### Step 9: 実行する

```powershell
python .\tools\run_case.py --run
```

`--run` は既存の実行ファイルを起動するだけで、CMakeキャッシュ生成、Visual Studio環境初期化、configure、コンパイル、リンクを行いません。

生成、ビルド、必要なテスト、実行をまとめて行う場合は次を使います。

```powershell
python .\tools\run_case.py --all
```

`--all` は毎回ビルドしてから実行します。反復計算では、最初に `--build` を1回実行し、その後は `--run` を使用します。`model.include_tests: false` の場合、`--all` でもテストは省略されます。

### Step 10: 結果を確認する

ソルバーの作業ディレクトリは次です。

```text
cases\case0001
```

標準の出力先は次です。

```text
cases\case0001\output
```

実行ログ、SLFファイル、タイミング結果などを確認します。SLFからVTI/PVDへの変換、逐次・MPI出力の使い分け、ParaViewでの密度・位相・量子渦表示は、[`後処理_ParaView可視化手順書.md`](後処理_ParaView可視化手順書.md)を参照します。

### NSEを同じフローで実行する

NSEでは、GPE用設計書の代わりに`environment.nse.yaml`をコピーします。

```powershell
$framework = "C:\Users\Owner\Documents\Codex\FrameWork"
$tool = "$framework\ScriptLibrary\RunEnvironment"
$designs = "$env:USERPROFILE\ResearchRuns\Designs"

New-Item -ItemType Directory -Force $designs | Out-Null
Copy-Item "$tool\environment.nse.yaml" `
  "$designs\nse_tgv.yaml"

python "$tool\prepare_environment.py" `
  "$designs\nse_tgv.yaml" --dry-run
python "$tool\prepare_environment.py" `
  "$designs\nse_tgv.yaml"
```

生成ログに表示された`nse_caseNNNN`へ移動し、GPEと同じコマンドを実行します。

```powershell
Set-Location "$env:USERPROFILE\ResearchRuns\nse_caseNNNN"

python .\tools\run_case.py --prepare
python .\tools\run_case.py --validate-only
python .\tools\run_case.py --build
python .\tools\run_case.py --run
```

NSEでは`case.yaml`から`input.dat`が生成されます。標準設計書は
`nse_cpu_mpi`、`nse_case`、`release`を選択し、`parallel`でMPIとOpenMPを
有効にします。
現在のNSEのMPI分割は4プロセス以上を必要とします。

最終保存ステップのファイル出力後、次が表示されればNSEは正常終了です。

```text
NSE calculation completed successfully: step=<final step>, time=<final time>
```

### NSE単一GPU版を実行する

NSEの単一GPU版でも、CPU/MPI版と同じ`environment.nse.yaml`を設計書の
ひな型にします。GPEと同様に、計算方式は設計書内の選択IDで切り替えます。

```powershell
$framework = "C:\Users\Owner\Documents\Codex\FrameWork"
$tool = "$framework\ScriptLibrary\RunEnvironment"
$designs = "$env:USERPROFILE\ResearchRuns\Designs"
$design = "$designs\nse_cuda.yaml"

New-Item -ItemType Directory -Force $designs | Out-Null
Copy-Item "$tool\environment.nse.yaml" $design
```

コピーした`nse_cuda.yaml`を開き、次の2項目を変更します。

```yaml
select:
  model: nse_cuda_single
  execution: release

parallel:
  use_mpi: false
  use_openmp: false
  use_cuda: true
```

```powershell
python "$tool\prepare_environment.py" $design --list-options model
python "$tool\prepare_environment.py" $design --dry-run
python "$tool\prepare_environment.py" $design
```

生成ログに表示された`nse_caseNNNN`へ移動し、入力生成、検証、ビルド、実行を
分けて行います。

```powershell
Set-Location "$env:USERPROFILE\ResearchRuns\nse_caseNNNN"

python .\tools\run_case.py --prepare
python .\tools\run_case.py --validate-only
python .\tools\run_case.py --build
python .\tools\run_case.py --run
```

設計書では、次の組合せが必要です。

```yaml
select:
  model: nse_cuda_single
  case: nse_case
  execution: release
```

単一GPU版では`parallel`を次のように指定します。MPIとOpenMPは使用せず、
CUDA Toolkitと対応するNVIDIAドライバを使います。`windows_gnu_msmpi`という
target名にMPIが含まれていますが、NSE単一GPUプロファイルではMPIライブラリを
リンクしません。このtargetはWindowsのGNU Fortran、Ninja、CUDA/MSVC環境を
まとめて指定するために使用します。

使用GPUを変更するときは、生成後の`cases\<case_id>\case.yaml`へ次を追加または
変更し、入力を再生成します。

```yaml
solver:
  cuda_device: 0
```

```powershell
python .\tools\run_case.py --prepare
python .\tools\run_case.py --run
```

開発時にCUDA回帰テストも生成環境へ含める場合は、環境設計書へ次を追加してから
環境を再生成します。

```yaml
model:
  include_tests: true
```

```powershell
python .\tools\run_case.py --build
python .\tools\run_case.py --test
```

現在のNSE単一GPU版は、`keep2`、`keep6`、`weno5z_roe`、`hybrid`の
対流流束、`central6`または`none`の粘性項、SSPRK3、周期境界に対応します。
初期条件生成とSLF出力はCPU側で行い、時間発展中の保存変数、周期境界、
CFL・拡散制限の評価、対流・粘性右辺、SSPRK3更新はGPU上で処理します。
出力時だけGPUからCPUへ場を戻します。

## 5. モデル別`environment`設計書の設定

`environment.gpe.yaml`と`environment.nse.yaml`は、どのモデルをどの計算機へ
展開するかを指定する設計書です。物理条件そのものは`case.yaml`に分離します。
引数を省略して`prepare_environment.py`を実行した場合は、GPE用の
`environment.gpe.yaml`が既定値として使われます。

### 5.1 選択肢カタログ

標準選択肢の正本は次です。

```text
ScriptLibrary\RunEnvironment\environment_options.yaml
```

現在の候補と追加方法は、次の資料でも確認できます。

```text
ScriptLibrary\RunEnvironment\ENVIRONMENT_OPTIONS.md
実行環境設計書_選択肢管理手順書.docx
実行環境設計書_選択肢管理台帳.xlsx
```

`source`、`destination`、`model`、`target`、`case`、`execution`、`scheduler`、`archive`を同じ仕組みで選択します。

| 分類 | 役割 | Windowsでの代表ID |
|---|---|
| `source` | ローカルFrameWorkとモデルカタログ | `local_framework` |
| `destination` | 外部実行環境の生成先 | `windows_research_runs` |
| `model` | NSE/GPEとソルバープロファイル | `nse_cuda_single`、`gpe_cpu_mpi_dft` |
| `target` | OS、コンパイラ、MPIの機械設定 | `windows_gnu_msmpi` |
| `case` | caseテンプレートと初期条件 | `gpe_quantum_taylor_green` |
| `execution` | Debug/Releaseなどのビルド構成 | `debug`、`release` |
| `scheduler` | ジョブ投入スクリプト | `disabled` |
| `archive` | 転送用アーカイブ | `none` |

MPI、OpenMP、CUDAの使用可否は`parallel`の真偽値で指定します。MPIプロセス数と
OpenMPスレッド数は、生成後の`case.yaml`へ記録します。

### 5.2 `select`

```yaml
select:
  source: local_framework
  destination: windows_research_runs
  model: gpe_cpu_mpi_fftw
  target: windows_gnu_msmpi
  case: gpe_quantum_taylor_green
  execution: release
  scheduler: disabled
  archive: none

parallel:
  use_mpi: true
  use_openmp: false
  use_cuda: false
```

選択IDは英数字で固定し、表示名や説明はカタログ側で管理します。caseとmodelなどの組合せ条件も検査されます。

### 5.3 個別値の上書き

候補の値を一部だけ変更するときは、同名セクションへ上書き値を書きます。

```yaml
destination:
  root: D:/ResearchRuns/gpe_case0042

execution:
  parallel_jobs: 16

case:
  id: case0042
  label: qtgv_resolution_study
```

解決順序は次のとおりです。

1. 標準カタログ
2. `option_catalogs`で指定した追加カタログ
3. `select`
4. 設計書内の同名セクション
5. コマンドライン上書き

後の段階ほど優先されます。生成環境には元設計書に加えて、最終値を記録した `environment.resolved.yaml` が保存されます。

### 5.4 追加カタログ

研究室や計算機固有の候補は、標準カタログを直接変更せず、別のYAMLへ追加できます。

```yaml
schema_version: 1
catalog_id: workstation_local
allow_override: false

options:
  destination:
    workstation_nvme:
      label: Workstation NVMe
      description: 高速なローカル作業領域
      values:
        root: E:/ResearchRuns/gpe_case0001
```

設計書から読み込みます。

```yaml
option_catalogs:
  - environment_options.local.yaml

select:
  destination: workstation_nvme
```

既存分類へ新しいIDを追加するだけなら、Pythonコードの変更は不要です。同じ分類とIDの重複はエラーになります。

### 5.5 一覧表示

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --list-options
```

分類を限定できます。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --list-options model
```

### 5.6 生成済み環境の置換

既存の生成済み環境を置き換える場合だけ `--overwrite` を使います。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --overwrite
```

> **保護動作:** `--overwrite` は `.generated_run_environment.json` を持つ生成済み環境だけを置換対象とします。手作業で作った任意のフォルダを出力先にしないでください。

## 6. モデルとプロファイルの選択

### 6.1 GPE

| model選択ID | ソルバープロファイル | 用途 | 主な依存関係 |
|---|---|---|---|
| `gpe_cpu_serial_dft` | `cpu_serial_dft` | 小規模確認、逐次CPU | Fortranコンパイラ |
| `gpe_cpu_serial_fftw` | `cpu_serial_fftw` | 逐次CPUとFFTW | FFTW |
| `gpe_cpu_mpi_dft` | `cpu_mpi_dft` | MPI動作確認、組込みDFT | MPI |
| `gpe_cpu_mpi_fftw` | `cpu_mpi_fftw` | MPI CPU計算 | MPI、FFTW |
| `gpe_cuda_single` | `cuda_single` | 単一GPU | CUDA、cuFFT |
| `gpe_cuda_mpi_cufftmp` | `cuda_mpi_cufftmp` | 複数GPU、分散FFT | CUDA、MPI、cuFFTMp |

最初の疎通確認には `cpu_serial_dft`、Windows上のMPI確認には `cpu_mpi_dft`、CPU本計算には利用環境に応じて `cpu_mpi_fftw` を使います。

### 6.2 NSE

NSEにはCPU MPI/OpenMP版、CPU MPI + 2DECOMP&FFT版、単一GPU版があります。

| model選択ID | ソルバープロファイル | 用途 | 主な依存関係 |
|---|---|---|---|
| `nse_cpu_mpi` | `cpu_mpi` | MPI、またはMPI + OpenMP | Fortranコンパイラ、MPI、必要に応じてOpenMP |
| `nse_cpu_mpi_2decomp_fftw` | `cpu_mpi_2decomp_fftw` | HIT初期化、分散FFT Forcing | Fortranコンパイラ、MPI、FFTW、2DECOMP&FFT |
| `nse_cuda_single` | `cuda_single` | 単一GPU | CUDA Toolkit、NVIDIAドライバ、CUDA対応C++ホストコンパイラ |

標準的なMPI + OpenMP計算では`environment.nse.yaml`を使用します。

```yaml
select:
  model: nse_cpu_mpi
  case: nse_case
  execution: release

parallel:
  use_mpi: true
  use_openmp: true
  use_cuda: false
```

初期条件、格子、時間刻み、対流流束、粘性項、周期境界、SSPRK3などの計算条件は、
生成後の`cases\<case_id>\case.yaml`で変更します。`input.dat`は
`run_case.py --prepare`で再生成されます。

単一GPU計算でも`environment.nse.yaml`を使用し、コピーした設計書で次を選択します。

```yaml
select:
  model: nse_cuda_single
  case: nse_case
  execution: release

parallel:
  use_mpi: false
  use_openmp: false
  use_cuda: true
```

GPU版では`keep2`、`keep6`、`weno5z_roe`、`hybrid`、
`central6`または`none`、SSPRK3、周期境界を利用できます。
非周期境界やMPI + CUDAは未対応であり、実行前の検証で停止します。

利用可能なプロファイルの正本は、次のモデルカタログです。

```text
ScriptLibrary\BuildSolver\model_catalog.yaml
```

## 7. 生成される実行環境

代表的な構成は次のとおりです。

```text
ResearchRuns\
  ├─ Designs\
  ├─ case_index.csv
  └─ gpe_case0001\
      ├─ .generated_run_environment.json
      ├─ environment.lock.json
      ├─ environment.source.yaml
      ├─ environment.resolved.yaml
      ├─ provenance.json
      ├─ README.md
      ├─ config\
      │   └─ machine.yaml
      ├─ ScriptLibrary\
      │   ├─ BuildSolver\
      │   └─ SetupCase\
      ├─ SolverLibrary\
      │   └─ GPE\
      ├─ templates\
      │   └─ case_template.yaml
      ├─ cases\
      │   └─ case0001\
      │       ├─ case.yaml
      │       ├─ input.nml
      │       ├─ notes.md
      │       └─ output\
      ├─ tools\
      │   ├─ run_case.py
      │   ├─ case_input.py
      │   ├─ global_case_index.py
      │   └─ yaml_support.py
      ├─ build\
      └─ submit.slurm
```

NSEでは`SolverLibrary\NSE`と`input.dat`、GPEでは`SolverLibrary\GPE`と
`input.nml`が配置されます。実際の構成と入力名は、選択したモデルの
`solver_manifest.yaml`に従います。

### 7.1 編集場所の原則

| 変更内容 | 編集する場所 |
|---|---|
| 配置先、モデル、MPI数 | ローカルに保存した `environment.gpe.yaml`または`environment.nse.yaml` |
| 流れ場、格子、物理量、時間、出力 | `cases\<case_id>\case.yaml` |
| 全モデル・全caseの条件一覧 | `${USERPROFILE}\ResearchRuns\case_index.csv`（自動更新） |
| コンパイラ、MPI、FFTW、CUDAの場所 | `config\machine.yaml` |
| ソルバーの実装 | ローカルFrameWorkの `SolverLibrary` |
| 共通ビルド機能 | ローカルFrameWorkの `ScriptLibrary` |
| 選択肢IDと設定値 | ローカルFrameWorkの `environment_options.yaml` |
| NASミラー | 同期ツールでローカルFrameWorkから反映 |

> **禁止事項:** 生成環境内のソースだけを修正して編集元としないでください。動作確認用の一時修正を行った場合は、確定後にローカルFrameWorkへ反映し、Git更新後にNASへ同期します。

## 8. case条件の変更と入力再生成

### 8.1 caseを変更する

次のファイルを編集します。

```text
cases\case0001\case.yaml
```

主な変更対象は次のとおりです。

- 格子数と計算領域
- 時間刻みとステップ数
- 物理パラメータ
- 初期条件の種類と初期条件固有のパラメータ
- NSEの対流流束、粘性項、境界条件、時間積分
- 実時間進行または虚時間進行
- 再開計算の入力
- 出力間隔と出力先

### 8.2 NSEの対流流束を選ぶ

NSEはCPU/MPI/OpenMP版と単一GPU CUDA版の両方で次の4方式を利用できます。
対流流束を選択する正本は`numerics.convective_scheme`だけです。

- `keep2`: 全領域で2次精度KEEP
- `keep6`: 全領域で6次精度KEEP（既定値）
- `weno5z_roe`: 全領域で特性空間の5次精度WENO-Z再構築とRoe流束
- `hybrid`: KEEPとWENO5-Z/Roeをセンサー値に応じて切り替え・連続混合

全領域でWENO5-Z/Roeを使う場合は次のように指定します。

```yaml
numerics:
  convective_scheme: weno5z_roe
```

滑らかな領域をKEEP6、衝撃波領域をWENO5-Z/Roeにする場合は
ハイブリッドを指定します。

```yaml
numerics:
  # keep2, keep6, weno5z_roe, hybrid
  convective_scheme: hybrid
  hybrid:
    smooth_scheme: keep6
    shock_scheme: weno5z_roe
    sensor: ducros_pressure
    sensor_onset: 0.01
    sensor_full: 0.10
```

非ハイブリッド計算では`convective_scheme`を`keep2`、`keep6`、または
`weno5z_roe`にします。その場合、`hybrid`以下の項目は計算にも生成後の
`input.dat`にも使用されません。`convective_scheme: keep6`ではWENO計算を
行いません。

旧`numerics.flux`と未使用だった`numerics.reconstruction`は廃止されています。
これらが残っている場合は黙って無視せず、`numerics.convective_scheme`への
移行を求めるエラーで停止します。

### 8.3 入力を自動生成する

```powershell
python .\tools\run_case.py --prepare
```

`run_case.py` は `environment.lock.json` からcase、モデル、プロファイル、入力ファイル名を取得します。GPEでは `input.nml`、NSEでは `input.dat` が生成されます。
同時に、現在の`case.yaml`の内容で外側の共通`case_index.csv`を更新します。

次のコマンドでも、`case.yaml` が入力ファイルより新しい場合は、処理開始前に自動再生成されます。

```powershell
python .\tools\run_case.py --validate-only
python .\tools\run_case.py --build
python .\tools\run_case.py --run
```

### 8.4 `case_input.py` を直接使う場合

通常は `run_case.py --prepare` を使用します。変換処理だけを個別に確認するときは、モデル、マニフェスト、プロファイルを明示します。

```powershell
python .\tools\case_input.py `
  --case .\cases\case0001\case.yaml `
  --manifest .\SolverLibrary\GPE\gp3d\solver_manifest.yaml `
  --model gpe `
  --profile cuda_single `
  --overwrite
```

`--profile` は `environment.lock.json` の値と一致させます。

## 9. 機械設定 `config/machine.yaml`

外部実行環境の `config\machine.yaml` は、その計算機固有の設定です。

確認する項目は次のとおりです。

- CMake generator
- Fortranコンパイラ
- MPIのinclude、library、launcher
- FFTWのルートまたはライブラリ
- CUDA Toolkit
- CUDAアーキテクチャ
- cuFFTMp
- 環境変数
- ビルド並列数

FFTWが自動検出されない場合は、`libraries.fftw_root` など、機械プロファイルで定義された項目にFFTWの配置先を指定します。

GPU計算では、計算機のGPUに対応する値を指定します。

```yaml
libraries:
  cuda_architectures: 86
```

NSE単一GPU版のWindowsビルドでは、CUDA C++部分のホストコンパイラとして
Visual Studio Build Toolsのx64環境も使用します。`run_case.py --build`が
`vcvars64.bat`を検出して初期化します。

> **分離の考え方:** モデル別の`environment.*.yaml`は「何をどこへ展開するか」、`machine.yaml`は「その計算機でどうコンパイルするか」、`case.yaml`は「何を計算するか」を担当します。

## 10. 検証、ビルド、実行コマンド

### 10.1 入力生成のみ

```powershell
python .\tools\run_case.py --prepare
```

### 10.2 検証のみ

```powershell
python .\tools\run_case.py --validate-only
```

### 10.3 CMake構成のみ

```powershell
python .\tools\run_case.py --configure
```

### 10.4 ビルド

```powershell
python .\tools\run_case.py --build
```

### 10.5 テスト

```powershell
python .\tools\run_case.py --test
```

### 10.6 実行のみ

実行前に`cases\<case_id>\case.yaml`で並列数を設定します。

```yaml
solver:
  use_mpi: true
  use_openmp: true
  use_cuda: false
  mpi_processes: 8
  omp_threads: 2
```

`use_mpi`、`use_openmp`、`use_cuda`は生成したソルバープロファイルと一致させます。
`mpi_processes`と`omp_threads`は、実行環境を再生成・再ビルドせず変更できます。

```powershell
python .\tools\run_case.py --run
```

`--run` は既存の実行ファイルを直接起動します。実行ファイルが存在しない場合は、先に `--build` を実行します。

### 10.7 ビルドと実行

```powershell
python .\tools\run_case.py --all
```

`--all` はビルド後に実行するため、実行だけを繰り返す用途には使用しません。

### 10.8 コマンドラインで一時的に上書きする

```powershell
python .\tools\run_case.py --run `
  --configuration Release `
  --processes 8 `
  --omp-threads 1
```

これは一時的な変更です。再現性を重視する本計算では、`case.yaml`の
`solver.mpi_processes`と`solver.omp_threads`へ同じ値を記録します。

### 10.9 実行せずコマンドだけ確認する

```powershell
python .\tools\run_case.py --run --dry-run
```

## 11. 結果と来歴の管理

### 11.1 計算結果

標準の計算結果は次に保存されます。

```text
cases\<case_id>\output
```

大規模計算では、出力先をデータ保存用NASや並列ファイルシステムへ切り替えることがあります。その場合も `case.yaml` に出力先を記録します。

### 11.2 来歴ファイル

| ファイル | 内容 |
|---|---|
| `environment.source.yaml` | 環境生成に使った元の設計書 |
| `environment.resolved.yaml` | 選択肢と上書きを反映した最終設計 |
| `environment.lock.json` | 選択ID、モデル、プロファイル、case |
| `provenance.json` | カタログSHA-256、生成元、Git、生成日時 |
| `generated\resolved_build.json` | CMakeへ渡した解決済みビルド設定 |
| `cases\<case_id>\case.yaml` | 計算条件 |

論文や報告書に使う計算では、結果データと一緒に少なくとも次を保存します。

- `case.yaml`
- `environment.source.yaml`
- `environment.resolved.yaml`
- `environment.lock.json`
- `provenance.json`
- `config\machine.yaml`
- 実行ログ
- 使用したソースのGitコミットID

## 12. ライブラリ更新後の再生成

ローカルFrameWorkの `SolverLibrary`、`ScriptLibrary`、選択肢カタログを更新した後は、外部実行環境を再生成します。

### Step 1: Gitと同期状態を確認する

```powershell
git -C C:\Users\Owner\Documents\Codex\FrameWork status
```

NASから取得すべき変更がある場合は、ローカル作業ツリーがクリーンであることを確認してから同期します。ローカルとNASの両方で同じファイルを変更した状態で同期を開始しません。

### Step 2: 既存caseと結果を退避する

必要な `case.yaml`、再開データ、結果、ログを別の保存場所へ退避します。

### Step 3: dry-runする

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --dry-run
```

### Step 4: 生成済み環境を置き換える

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_case0001.yaml" `
  --overwrite
```

### Step 5: caseを反映して再検証する

退避した条件を新しい `case.yaml` に反映し、入力を再生成してから `--validate-only` を実行します。

> **注意:** 新旧バージョンでcaseスキーマが変わった場合、古い `case.yaml` をそのまま上書きせず、差分を確認して必要な値だけ移します。

### Step 6: GitHubとNASへ反映する

テスト済みのソース、設計書、手順書をGitへ記録し、GitHubへpushします。その後、ローカルFrameWorkからNASミラーへ同期します。

```text
ローカル編集・テスト
  -> Git commit / GitHub push
  -> ローカルFrameWorkからNASへ同期
```

GitHubは変更履歴、NASはファイルミラーです。NAS同期だけでGitの履歴管理を代用しません。

## 13. スパコンでの実行

スパコン向けの実行環境も、ローカルFrameWorkから生成します。スパコンやNAS上のSolverLibraryを直接ビルドせず、必要なソース、設定、case、実行スクリプトをまとめた転送用アーカイブを作成します。

```text
ローカルFrameWork
  -> 選択肢を解決
  -> 外部実行環境を生成
  -> 転送用アーカイブを作成
  -> スパコンへ転送
  -> 入力生成・検証
  -> ビルドを1回実行
  -> Slurmジョブでは実行のみ
```

### 13.1 スパコン向け設計書を作成する

`environment.hpc.yaml` をローカルの設計書置場へコピーし、選択肢IDとcase固有値を編集します。

```powershell
Copy-Item "$tool\environment.hpc.yaml" `
  "$designs\gpe_hpc.yaml"
```

```yaml
select:
  source: local_framework
  destination: windows_research_runs
  model: gpe_cpu_mpi_fftw
  target: linux_hpc_slurm
  case: gpe_quantum_taylor_green
  execution: release
  scheduler: slurm
  archive: gztar

parallel:
  use_mpi: true
  use_openmp: false
  use_cuda: false

execution:
  parallel_jobs: 8

scheduler:
  job_name: gpe-case0001
  time_limit: 01:00:00
  modules:
    - gcc
    - openmpi
    - fftw
```

複数GPUとcuFFTMpを使う場合は、モデルとGPU資源を変更します。

```yaml
select:
  model: gpe_cuda_mpi_cufftmp

scheduler:
  job_name: gpe-cufftmp

parallel:
  use_mpi: true
  use_openmp: false
  use_cuda: true
```

利用可能なIDはローカルで確認できます。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_hpc.yaml" `
  --list-options

python "$tool\prepare_environment.py" `
  "$designs\gpe_hpc.yaml" `
  --list-options target
```

### 13.2 ローカルで転送用アーカイブを作る

まず、コピー内容と解決済み設定を確認します。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_hpc.yaml" `
  --dry-run
```

問題がなければ、実行環境とアーカイブを生成します。

```powershell
python "$tool\prepare_environment.py" `
  "$designs\gpe_hpc.yaml" `
  --archive `
  --archive-format gztar
```

`windows_research_runs`が空いている番号を自動採番します。以下では生成結果が
`gpe_case0001`だったものとして記載します。コマンド出力が別の番号を示した場合は、
以降のパスもその番号へ読み替えてください。

生成された `environment.resolved.yaml`、`environment.lock.json`、`provenance.json` を確認し、選択したモデル、プロファイル、MPI数、カタログのハッシュが意図どおりか確認します。

### 13.3 スパコンへ転送して展開する

生成した `.tar.gz` を `scp`、WinSCPなどでスパコンへ転送します。PowerShellから転送する例です。

```powershell
scp "$env:USERPROFILE\ResearchRuns\gpe_case0001.tar.gz" `
  USER@SUPERCOMPUTER:~
```

ログイン後、スパコンの作業領域へ展開します。

```bash
mkdir -p "$SCRATCH/gpe_case0001"
tar -xzf ~/gpe_case0001.tar.gz \
  -C "$SCRATCH/gpe_case0001" \
  --strip-components=1
cd "$SCRATCH/gpe_case0001"
```

### 13.4 ソフトウェアモジュールを読み込む

CPU、MPI、FFTW計算の例です。

```bash
module purge
module load gcc
module load openmpi
module load cmake
module load fftw
```

CUDA、cuFFTMp計算の例です。

```bash
module purge
module load nvhpc
module load cuda
module load openmpi
module load cufftmp
module load nvshmem
module load cmake
```

モジュール名と組合せは利用機関ごとに異なります。`module avail`、利用手引き、サポート情報を優先してください。

### 13.5 `machine.yaml` をスパコンへ合わせる

展開後、スパコン固有のコンパイラ、MPIランチャー、FFTW、CUDA、cuFFTMp、NVSHMEMの場所を確認します。

```bash
vi config/machine.yaml
```

主な確認項目は次のとおりです。

- `compiler.fortran`
- `tools.cmake`
- `tools.mpi_launcher`
- `tools.mpi_process_option`
- `libraries.fftw_root`
- `libraries.cuda_toolkit_root`
- `libraries.cuda_architectures`
- `libraries.cufftmp_root`
- `libraries.nvshmem_root`

GPUアーキテクチャの代表例です。

| GPU | `cuda_architectures` |
|---|---:|
| NVIDIA A100 | `80` |
| NVIDIA RTX 3050 | `86` |
| NVIDIA H100 | `90` |
| NVIDIA B100/B200 | 利用機関のCUDA環境に従う |

例えばH100では次のように指定します。

```yaml
libraries:
  cuda_architectures: 90
```

> **重要:** 異なるGPU向けのPTXへ実行時JITを任せず、使用するGPUのアーキテクチャを明示します。Toolkitとドライバの対応差による `unsupported toolchain` を避けるうえでも重要です。

### 13.6 入力生成、検証、ビルド

`case.yaml` からソルバー入力を生成し、設定を検証します。

```bash
python3 tools/run_case.py --prepare
python3 tools/run_case.py --validate-only
```

ビルドは原則として1回だけ実行します。

```bash
python3 tools/run_case.py --build
```

ログインノードでコンパイルできない場合は、利用機関の規則に従い、インタラクティブジョブ、ビルドノード、またはビルド専用バッチジョブ内で実行します。

### 13.7 Slurmジョブでは実行だけを行う

本計算用 `submit.slurm` では、次のコマンドだけを実行します。

```bash
python3 tools/run_case.py --run
```

`submit.slurm`はMPIプロセス数を`SLURM_NTASKS`、OpenMPスレッド数を
`SLURM_CPUS_PER_TASK`から取得します。並列数は投入コマンドで指定します。

`--run` は既存の実行ファイルを起動し、CMake configureやビルドを行いません。`--all` は毎回ビルドするため、反復するSlurmジョブには使用しません。

投入前にジョブスクリプトを確認してから投入します。

```bash
less submit.slurm
sbatch --nodes=1 --ntasks-per-node=8 --cpus-per-task=2 submit.slurm
squeue -u "$USER"
```

ログは次のように確認します。

```bash
tail -f slurm-<jobid>.out
```

### 13.8 Slurm設定の確認項目

投入時に次を利用機関の仕様へ合わせます。

- `account`
- `partition`
- `nodes`
- `--ntasks-per-node`
- `--cpus-per-task`
- GPU利用時の`--gpus-per-node`
- `gpus_per_node`
- `time_limit`
- `modules`
- MPI起動方法

GPU計算では、ジョブ資源のGPU数、MPIプロセス数、選択したプロファイルを一致させます。1ランク1GPUの構成では、MPIプロセス数と割り当てGPU数を同数にします。

> **運用原則:** NASがスパコンから見える場合でも、NAS上のファイルを計算ノードから直接ビルド・実行する運用は避けます。ジョブごとに固定した実行環境をスパコンの作業領域へ展開することで、再現性とI/O性能を保ちます。

## 14. よくある問題

### 14.1 `python` が見つからない

```text
python : 用語 'python' は認識されません
```

対処:

```powershell
py --version
py "$tool\prepare_environment.py" "$designs\gpe_case0001.yaml"
```

またはPython実行ファイルの絶対パスを指定します。

### 14.2 UNCパスでCMDが起動できない

```text
UNC パスはサポートされません
```

NAS上を作業ディレクトリにしてCMDベースのビルドを実行したことが原因です。本手順では、編集元を `C:\Users\Owner\Documents\Codex\FrameWork`、生成先を `${USERPROFILE}\ResearchRuns\...` とし、NASは同期ミラーに限定します。

### 14.3 FFTWが見つからない

```text
FFTW backend selected, but FFTW3 library was not found
```

対処:

- FFTWを対象計算機へ導入する。
- `config\machine.yaml` にFFTWのルートまたはライブラリを指定する。
- 疎通確認だけならDFTプロファイルへ切り替える。

### 14.4 MPI実行に失敗する

確認事項:

- `mpiexec` または `mpirun` が使える。
- コンパイル時と実行時のMPI実装が一致している。
- `case.yaml`の`solver.mpi_processes`が格子分割条件を満たす。
- NSEのMPI実行では、並列前提の構成に1プロセスを指定していない。

### 14.5 NSEで出力間隔に関する異常が出る

`output_frequency` は正の値ならその間隔で保存し、`0`以下ならステップ出力を
無効化します（`write_initial: true`の初期出力は別です）。`nsteps: 1000`、
`output_frequency: 1`または`100`では、1000ステップ目のファイル書き込み後に
正常終了メッセージが表示されます。

`IEEE_UNDERFLOW_FLAG`や`IEEE_DENORMAL`は非正規化数または極小値が生じたことを示し、
それだけで発散とは判定しません。現行版では、最終出力、ログの有限値、
`NSE calculation completed successfully`の有無を合わせて正常終了を確認します。

### 14.6 変更した条件が実行へ反映されない

`input.dat` または `input.nml` を直接編集せず、`case.yaml` を編集した後に次を実行します。

NSEの対流方式は`numerics.convective_scheme`へ指定します。旧`numerics.flux`または
`numerics.reconstruction`に指定すると、現行版は移行エラーを表示します。

```powershell
python .\tools\run_case.py --prepare
python .\tools\run_case.py --validate-only
```

検証、ビルド、実行でも `case.yaml` の方が入力ファイルより新しければ自動再生成されます。

### 14.7 出力先に既存フォルダがある

必要な結果を退避してから `prepare_environment.py --overwrite` を使います。生成マーカーがないフォルダは、誤削除防止のため置換されません。

### 14.8 CMakeのパスが長すぎる

標準の`${USERPROFILE}\ResearchRuns`より短い場所が必要な場合は、
`destination.root`と`destination.case_index`を同じ親ディレクトリへ変更します。

```yaml
destination:
  root: D:/Runs
  case_index: D:/Runs/case_index.csv
```

### 14.9 選択肢IDが見つからない

```text
Unknown option ID
```

対処:

```powershell
python "$tool\prepare_environment.py" DESIGN.yaml --list-options
python "$tool\prepare_environment.py" DESIGN.yaml --list-options model
```

`environment_options.yaml` または設計書で指定した追加カタログを確認します。IDの表示名ではなく、`select` に記載するIDを使います。

### 14.10 ローカルとNASで同期競合が起きた

同期をいったん停止し、ローカルとNASの両方を保存して差分を確認します。更新日時だけで一方を上書きせず、Gitのコミット履歴と内容を基準に採用する版を決めます。

> **注意:** FrameWorkで編集中、Git操作中、Word・Excel編集中、ビルド中に双方向同期を走らせないでください。

### 14.11 CUDAでPTXツールチェーンエラーが出る

```text
the provided PTX was compiled with an unsupported toolchain
```

主な原因は、CUDA Toolkit、NVIDIAドライバ、GPUアーキテクチャの組合せです。

確認します。

```bash
nvidia-smi
nvcc --version
```

`config/machine.yaml` の `cuda_architectures` を実際のGPUへ合わせ、クリーンビルドします。

```yaml
libraries:
  cuda_architectures: 86
```

```bash
python3 tools/run_case.py --build --clean-first
```

Toolkitがドライバの対応範囲より新しい場合は、利用機関が提供する互換モジュールへ揃えるか、管理者へドライバ更新を依頼します。

### 14.12 NSE単一GPU版でプロファイルの組合せエラーが出る

次の選択を確認します。

```yaml
select:
  model: nse_cuda_single
  execution: release
```

`parallel.use_cuda: true`、`parallel.use_mpi: false`、
`parallel.use_openmp: false`であることを確認してください。生成済みの
`case.yaml`でも同じ使用可否になり、`solver.mpi_processes: 1`になります。

### 14.13 NSE単一GPU版で未対応スキームのエラーが出る

現時点のCUDA版で使用できる代表的な組合せは次のとおりです。

```yaml
numerics:
  # keep2, keep6, weno5z_roe, hybrid
  convective_scheme: hybrid
  hybrid:
    smooth_scheme: keep6
    shock_scheme: weno5z_roe
    sensor: ducros_pressure
    sensor_onset: 0.01
    sensor_full: 0.10
  viscous_scheme: central6
  boundary_condition: periodic
  time_integrator: ssprk3
```

`viscous_scheme: none`も選択できます。非周期境界とMPI + CUDAは未対応です。
別の条件を試す場合はCPU版へ切り替えるか、CUDAバックエンド側へ
対応実装を追加します。

## 15. 日常運用チェックリスト

### 計算前

- ローカルFrameWorkが最新で、同期競合がない。
- 使用するGitコミットが確定している。
- `--list-options` で選択肢IDを確認した。
- 使用モデルに対応する`environment.gpe.yaml`または`environment.nse.yaml`の`select`、case、出力先が正しい。
- `prepare_environment.py --dry-run` の内容が正しい。
- `case.yaml` の格子、時間刻み、物理条件、初期条件が正しい。
- NSEの`convective_scheme`とハイブリッド設定が意図した方式になっている。
- NSEのcaseに旧`numerics.flux`と`numerics.reconstruction`が残っていない。
- `run_case.py --prepare` で入力生成が成功する。
- `machine.yaml` のコンパイラ、MPI、FFT、CUDA設定が正しい。
- GPU計算では `cuda_architectures` が割り当てGPUと一致している。
- `run_case.py --validate-only` が成功する。
- 必要なディスク容量と計算時間を見積もった。

### 計算中

- 標準出力とエラー出力を保存している。
- ノルム、エネルギー、密度などの監視量が異常でない。
- 出力容量が想定内である。
- MPI/GPUの利用率とタイミングを確認している。

### 計算後

- 最終出力と`NSE calculation completed successfully`を確認した。
- 結果とログを保存した。
- 設計書、case、machine設定、来歴ファイルを保存した。
- 可視化用データを生成し、初期・中間・最終時刻を確認した。
- 再現に必要なGitコミットIDを記録した。
- 確定したソースと文書をGitHubへ反映した。
- Word・Excel・ビルド処理を終了してからローカルFrameWorkをNASへ同期した。

## 16. コマンド早見表

### Windows

```powershell
# 基準パス
$framework = "C:\Users\Owner\Documents\Codex\FrameWork"
$tool = "$framework\ScriptLibrary\RunEnvironment"
$design = "$env:USERPROFILE\ResearchRuns\Designs\gpe_case0001.yaml"

# GPE用設計書を作る場合
Copy-Item "$tool\environment.gpe.yaml" "$design"

# NSE用設計書を作る場合は、上の2行の代わりに次を使う
# $design = "$env:USERPROFILE\ResearchRuns\Designs\nse_case0001.yaml"
# Copy-Item "$tool\environment.nse.yaml" "$design"

# NSE単一GPU用設計書を作る場合
# $design = "$env:USERPROFILE\ResearchRuns\Designs\nse_cuda.yaml"
# Copy-Item "$tool\environment.nse.yaml" "$design"
# コピー後、select.modelをnse_cuda_single、
# select.executionをreleaseへ変更し、parallelでCUDAだけをtrueにする

# 選択肢の一覧
python "$tool\prepare_environment.py" "$design" --list-options
python "$tool\prepare_environment.py" "$design" --list-options model

# 実行環境の事前確認
python "$tool\prepare_environment.py" "$design" --dry-run

# 実行環境の生成
python "$tool\prepare_environment.py" "$design"

# 生成済み実行環境の置換
python "$tool\prepare_environment.py" "$design" --overwrite

# case.yamlから入力を生成
python .\tools\run_case.py --prepare

# 設定検証
python .\tools\run_case.py --validate-only

# ビルド
python .\tools\run_case.py --build

# 既存バイナリで実行のみ
python .\tools\run_case.py --run

# ビルドしてから実行
python .\tools\run_case.py --all

# 実行専用コマンドの表示
python .\tools\run_case.py --run --dry-run
```

### Linux / Slurm

```bash
# 転送済みアーカイブを展開
mkdir -p "$SCRATCH/gpe_case0001"
tar -xzf gpe_case0001.tar.gz -C "$SCRATCH/gpe_case0001" --strip-components=1
cd "$SCRATCH/gpe_case0001"

# 利用機関のモジュールを読み込む
module purge
module load gcc openmpi cmake fftw

# 入力生成
python3 tools/run_case.py --prepare

# 設定検証
python3 tools/run_case.py --validate-only

# ビルドは1回
python3 tools/run_case.py --build

# バッチジョブ投入
sbatch --nodes=1 --ntasks-per-node=8 --cpus-per-task=2 submit.slurm

# ジョブ確認
squeue -u "$USER"

# ログ確認
less slurm-<jobid>.out
```

`submit.slurm` の本計算コマンドは `python3 tools/run_case.py --run` とし、ジョブごとの再ビルドを避けます。

## 17. 責任分界のまとめ

| ファイルまたは場所 | 主な責任 |
|---|---|
| ローカルFrameWorkの `SolverLibrary` | ソルバー実装の編集元 |
| ローカルFrameWorkの `ScriptLibrary` | case生成、ビルド、実行環境生成の編集元 |
| `environment_options.yaml` | 選択肢IDと展開値の標準カタログ |
| `${USERPROFILE}\ResearchRuns\Designs\*.yaml` | 選択肢ID、case固有値、一時上書き |
| `${USERPROFILE}\ResearchRuns\case_index.csv` | 全モデル・全caseの共通台帳 |
| `config\machine.yaml` | 計算機固有のツールチェーン |
| `cases\<id>\case.yaml` | 物理条件、初期条件、実行時のMPI/OpenMP並列数 |
| `input.dat` / `input.nml` | ソルバーが読む生成物 |
| `cases\<id>\output` | 計算結果 |
| 来歴JSON/YAML | 再現性と追跡性 |
| GitHub | 変更履歴と共有 |
| NASのFrameWork | ローカルFrameWorkの同期ミラー |

## 18. ローカル・GitHub・NAS同期の手順

### 18.1 作業開始時

1. ローカルFrameWorkで `git status` を確認します。
2. 他端末で更新した内容がある場合だけ、NASまたはGitHubからローカルへ取り込みます。
3. 取り込み後に差分を確認し、ローカルの編集を始めます。
4. 外部実行環境はローカルFrameWorkから生成します。

```powershell
git -C C:\Users\Owner\Documents\Codex\FrameWork status
git -C C:\Users\Owner\Documents\Codex\FrameWork pull --ff-only
```

### 18.2 作業終了時

1. テストと文書確認を完了します。
2. Gitへコミットし、GitHubへpushします。
3. Word、Excel、エディター、ビルド処理を終了します。
4. ローカルFrameWorkからNASミラーへ同期します。
5. 同期ツールの比較結果で未反映・競合がないことを確認します。

```powershell
git -C C:\Users\Owner\Documents\Codex\FrameWork status
git -C C:\Users\Owner\Documents\Codex\FrameWork push
```

### 18.3 同期対象

| 種類 | 方針 |
|---|---|
| `SolverLibrary`、`ScriptLibrary` | Git記録後にNASへ同期 |
| 設計書、手順書、テンプレート | Git記録後にNASへ同期 |
| `build`、一時ファイル、キャッシュ | 原則として同期しない |
| 大規模な計算結果 | データ保存用領域へ保存し、FrameWorkへ混在させない |
| 外部実行環境 | 必要なら設計書と来歴だけを保存し、再生成を基本とする |

> **同期方向:** 通常は「ローカルからNAS」です。別端末で更新した場合は、作業開始前にGitHubまたはNASからローカルへ取り込みます。同じファイルを両側で編集してから自動同期する運用は避けます。
